#include "bwt.hpp"

// TODO(kevintownsend): Complete.

std::pair<std::vector<char>, char*> bwt::charBwt(char* first, char* last) {
  size_t size = last - first;
  return std::make_pair(std::vector<char>(), nullptr);
}
